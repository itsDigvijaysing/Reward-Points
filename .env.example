# Local-only secrets for dev scripts (NOT bundled into the app).
# Copy to .env and fill in real values. The .env file is gitignored.
#
# Get a free Gemini API key from https://aistudio.google.com/apikey
GOOGLE_API_KEY=
